/*
 * Optional local browser configuration.
 * Copy this file to config.js. config.js is gitignored and is loaded only by
 * your browser; prefer entering a short-lived user ID token in the UI.
 */
window.PICO_APP_CONFIG = {
  firebase: {
    databaseUrl: "",
    deviceId: "pico-w-01",
    authToken: "",
  },
};
